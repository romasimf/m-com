<% if resource.errors.any? %>
  <div class="mb-3 rounded-2xl border border-red-300/30 bg-red-900/35 px-4 py-3 text-sm text-red-100" data-turbo-temporary>
    <% resource.errors.full_messages.each do |message| %>
      <p>• <%= message %></p>
    <% end %>
  </div>
<% end %>
